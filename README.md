# Python-HTML-website
  A Python with HTML website uses `app.py` to run the server and handle backend logic. HTML files in the `templates` folder create the user interface. The `static` folder stores CSS and JavaScript files for styling and interactivity, making the website structured, functional, and user-friendly
